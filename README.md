# 🎓 Учебный Портал (Student Portal)

**Веб-приложение на Flask для организации учебного процесса.**

Система позволяет студентам иметь централизованный доступ к лекциям, умному расписанию и дедлайнам, а преподавателям — управлять учебным контентом через защищенную административную панель без вмешательства в программный код.

🔗 **LIVE Демо:** [https://cmbsu.pythonanywhere.com/](https://cmbsu.pythonanywhere.com/)

---

## 🚀 Функциональность

### 👨‍🎓 Для студентов:
* **📚 Лекции:** Автоматическая сортировка по семестрам, предпросмотр и скачивание PDF.
* **📅 Умное расписание:** Генерация дат занятий на весь семестр вперед (учитывает день недели, "мигающие" недели и праздники).
* **⏳ Дедлайны и Лабы:**
    * Разделение на Лабораторные и Контрольные работы.
    * Статусы: `Активно`, `Просрочено` (автоматический расчет).
    * Прямая ссылка на скачивание файла задания.
* **🌙 Интерфейс:** Поддержка темной темы и адаптивный дизайн.

### 👨‍🏫 Для преподавателя (Админ-панель):
* **🔐 Безопасность:** Доступ по специальному коду доступа (токену).
* **📤 Загрузка контента:** Drag-and-drop загрузка лекций и методичек (без использования FTP).
* **🛠 Конструктор:** Удобная форма создания дедлайнов с привязкой файлов.
* **📝 Редактор данных:** Прямое редактирование JSON-конфигов расписания.

---

## 💻 Инструкция по локальному запуску (Local Development)

Чтобы запустить проект на своем компьютере, следуйте инструкции:

### Предварительные требования
* Python 3.8+
* Git (опционально)

### 1. Клонирование и подготовка
Скачайте архив с проектом или клонируйте репозиторий:

```bash
git clone <ссылка_на_ваш_репозиторий>
cd my_project
```

### 2. Создание виртуального окружения
Рекомендуется использовать виртуальное окружение, чтобы не конфликтовать с системными библиотеками.

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**macOS / Linux:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 4. Настройка (Опционально)
Если в проекте используется файл конфигурации (например, `.env` или настройки внутри `app.py`), убедитесь, что задан `SECRET_KEY` и код доступа к админке.

### 5. Запуск
```bash
python app.py
```
После запуска сайт будет доступен в браузере по адресу:  
👉 `http://127.0.0.1:5000`

---

## ☁️ Инструкция по деплою и поддержке (PythonAnywhere)

Проект развернут на хостинге PythonAnywhere. Ниже описаны действия для администратора сервера.

### Обновление кода (Redeploy)
Если вы внесли изменения в код локально и хотите обновить сайт:

1. **Зайдите** в панель управления на [www.pythonanywhere.com](https://www.pythonanywhere.com/).
2. Откройте **Bash Console**.
3. Перейдите в папку проекта и заберите свежие изменения (если используется Git):
   ```bash
   cd mysite
   git pull origin main
   ```
   *(Или загрузите новые файлы `app.py` / `templates` вручную через вкладку **Files**).*
4. Перейдите на вкладку **Web**.
5. Нажмите большую зеленую кнопку **Reload <ваш_домен>**.

### Управление файлами на сервере
Все загруженные через админку файлы сохраняются в папке `mysite/static/`.
* `static/lectures/` — лекции.
* `static/labs/` — задания к лабам.
* `static/materials/` — доп. материалы.

Вы можете делать бэкап этих папок через вкладку **Files** на хостинге.

---

## ⚙️ Руководство по Админ-панели

Административная панель находится по адресу:  
👉 **`/admin`**

**🔐 Доступ:** Для входа требуется ввести **30-значный секретный код**.

### Сценарии использования:

#### 1. Добавление новой лекции
1. В блоке **"Загрузка новых файлов"** выберите категорию (например, `1_sem`).
2. Нажмите "Выберите файл" и укажите PDF на компьютере.
3. Нажмите **"Загрузить"**. Файл моментально появится на сайте.

#### 2. Создание лабораторной работы
1. **Загрузите задание:** В блоке загрузки выберите категорию `Файлы к Лабам (labs)` и загрузите методичку.
2. **Создайте запись:**
   * Прокрутите к блоку **"Конструктор задач"**.
   * Укажите название предмета, номер лабы и дедлайн.
   * В поле **"Прикрепить файл"** выберите загруженный на 1 шаге документ.
   * Нажмите **"+ Добавить"**.

#### 3. Настройка расписания (Семестр)
Расписание строится на базе JSON.
1. Найдите поле **"Настройки Расписания (JSON)"**.
2. В объекте предмета найдите поле `first_date`.
3. Укажите дату **первого занятия** в семестре (формат `YYYY-MM-DD`).
4. Система сама рассчитает все даты на полгода вперед, учитывая шаг недели (числитель/знаменатель).

---

## 📂 Структура проекта

```text
my_project/
├── app.py                # Точка входа (Backend Logic)
├── requirements.txt      # Список зависимостей
├── lectures.json         # Метаданные лекций (генерируется)
├── schedule.json         # База данных расписания
├── deadlines.json        # База данных дедлайнов
├── templates/            # HTML-шаблоны (Jinja2)
│   ├── base.html         # Основной каркас
│   ├── index.html        # Главная страница
│   ├── admin.html        # Админ-панель
│   └── ...
└── static/               # Статика
    ├── css/              # Стили
    ├── lectures/         # Хранилище PDF лекций
    ├── materials/        # Хранилище доп. материалов
    └── labs/             # Хранилище заданий
```
```

---
